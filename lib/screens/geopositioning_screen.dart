import 'package:flutter/material.dart';

class GeopositioningScreen extends StatelessWidget {
  const GeopositioningScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Geopositioning"),
      ),
    );
  }
}
