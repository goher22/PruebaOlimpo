import 'package:flutter/material.dart';

class StatusSemaphoreScreen extends StatelessWidget {
  const StatusSemaphoreScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("StatusSemaphore"),
      ),
    );
  }
}
