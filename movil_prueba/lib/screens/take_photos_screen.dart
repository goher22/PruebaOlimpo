import 'package:flutter/material.dart';

class TakePhoteScreen extends StatelessWidget {
  const TakePhoteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("TakePhotos"),
      ),
    );
  }
}
