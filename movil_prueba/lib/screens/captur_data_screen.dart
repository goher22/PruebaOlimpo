import 'package:flutter/material.dart';

class CapturDataScreen extends StatelessWidget {
  const CapturDataScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("CapturData"),
      ),
    );
  }
}
